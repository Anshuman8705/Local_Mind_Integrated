# Single-host production stack. Copy backend/.env.example to deploy/.env and
# set DJANGO_SECRET_KEY, DJANGO_ALLOWED_HOSTS, POSTGRES_PASSWORD and
# INITIAL_USER_PASSWORD at minimum. Then: docker compose up -d --build
services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_DB: localmind
      POSTGRES_USER: localmind
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD in deploy/.env}
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U localmind"]
      interval: 10s
      timeout: 5s
      retries: 10

  ollama:
    image: ollama/ollama:latest
    restart: unless-stopped
    environment:
      OLLAMA_KEEP_ALIVE: ${OLLAMA_KEEP_ALIVE:-30m}
      OLLAMA_NUM_PARALLEL: ${OLLAMA_NUM_PARALLEL:-1}
    volumes:
      - ollama:/root/.ollama
    # Uncomment for an NVIDIA GPU (needs the NVIDIA container toolkit):
    # deploy:
    #   resources:
    #     reservations:
    #       devices: [{ driver: nvidia, count: all, capabilities: [gpu] }]

  api:
    build: ../backend
    restart: unless-stopped
    env_file: .env
    environment:
      DATABASE_URL: postgres://localmind:${POSTGRES_PASSWORD}@db:5432/localmind
      OLLAMA_BASE_URL: http://ollama:11434
      MEDIA_ROOT: /app/media
      DJANGO_DEBUG: "false"
    volumes:
      - media:/app/media
      - static:/app/staticfiles
    depends_on:
      db: { condition: service_healthy }
      ollama: { condition: service_started }

  # Periodic maintenance: requeue documents abandoned by a recycled worker and
  # flush expired JWT blacklist rows. Runs every 15 minutes.
  maintenance:
    build: ../backend
    restart: unless-stopped
    env_file: .env
    environment:
      DATABASE_URL: postgres://localmind:${POSTGRES_PASSWORD}@db:5432/localmind
      OLLAMA_BASE_URL: http://ollama:11434
      MEDIA_ROOT: /app/media
      DJANGO_DEBUG: "false"
      AI_ENABLED: "false"
    volumes:
      - media:/app/media
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        while true; do
          python manage.py requeue_stuck_documents || true
          python manage.py flushexpiredtokens || true
          sleep 900
        done
    depends_on:
      db: { condition: service_healthy }

  web:
    image: nginx:1.27-alpine
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - static:/srv/static:ro
    depends_on:
      - api

volumes:
  pgdata:
  ollama:
  media:
  static:
